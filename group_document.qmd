---
title: "Group Document"
format: html
editor: visual
---

### s132347 (Sofie) section:

```{r}

print("Hello World")
```

#### New feature

Here is some random text that I am supposed to write.

Here is a code:

```{r}

print(1+2)
```

### s252750 (Sylvia) section

```{r}
xy <- seq(from = -3,
          to = 3, 
          by = 0.01)
expand.grid(x = xy,
            y = xy) |>
  ggplot(
    mapping = aes(
      x = (1 - x - sin(y^2)),
      y = (1 + y - cos(x^2)))) +
  geom_point(alpha = 0.05,
             shape = 20,
             size = 0) +
  theme_void() +
  coord_polar()
```

### New feature

```{r}
gravier_clean <- gravier |>
  bind_cols() |>
  as_tibble()
```

### s253710 (Henrique) section

```{r}
print('Hi')
```

#### New Feature

This code prints the word 'Hello'

```{r}
print('Hello')
```

### s253689 (Paula) section
```{r}
print("Hello")
```

